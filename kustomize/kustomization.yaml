apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - service.yaml

patches:
  - path: ue-deployment-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: free5gc-zebra-ueransim-ue
